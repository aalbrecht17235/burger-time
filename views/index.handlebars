<h1>Burgs!</h1>

<h2>Burgers Burgers Burgers</h2>

<div> Available Burgers
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers devour=true}}
  {{/unless}}
  {{/each}}
<div>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>

 <div> <p>Eaten Burgers</p>
          {{#each burgers}}
          {{#if devoured}}
          {{> eaten}}
          {{/if}}
          {{/each}}
        </div>