<div class="available_burgers">
    {{id}}.
    {{burger_name}}
</div>

<div class="devour">
		<button id="devour" class="btn btn-danger" data-id="{{id}}" data-newdevour="{{devour}}">
			Devour Burger
		</button>
	</div>
</div>